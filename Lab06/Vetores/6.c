#include <stdio.h>

int main(void) {
  //primo eh um numero divisivel por 1 e por ele mesmo.

  int array[6];
  for(int i = 0; i<6; i++){
    printf("Insira um numero para array[%d]: \n", i);
    scanf("%d", &array[i]);
  }

  for(int j = 0; j<6; j++){
    if()
  }
  
  return 0;
}